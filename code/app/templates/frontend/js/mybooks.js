(function () {
		
	MyBooks = function (settings) {  
		var defaults = {};
		
		settings = $.extend(defaults, settings);
		
		/////////////////////////////////
		
		this.initilize = (function () {
		})();
				
		return this;
	};

})();